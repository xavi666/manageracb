csv << [
  "seconds",
  "points",
  "rebounds",
  "assists",
  "value"
]

@statistics.each do |statistic|
  if statistic.seconds > 0
    csv << [
      statistic.seconds, 
      statistic.points,
      statistic.rebounds,
      statistic.assists,
      statistic.value
    ]
  end
end