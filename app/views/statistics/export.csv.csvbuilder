csv << [
  "seconds",
  "points",
  "two_p",
  "two_pm",
  "value"
]

@statistics.each do |statistic|
  if statistic.seconds > 0
    csv << [
      statistic.seconds, 
      statistic.points,
      statistic.two_p,
      statistic.two_pm,
      statistic.value
    ]
  end
end